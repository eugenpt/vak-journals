/**
 * Site settings (edit and commit, or set in repo before deploy).
 *
 * Yandex Metrika: https://metrika.yandex.ru/ → add counter for your Pages URL
 * (e.g. https://eugenpt.github.io/vak-journals/). Copy the numeric counter ID.
 *
 * Leave null to disable analytics (default).
 */
window.VAK_SITE = {
  metrikaId: null,
};
